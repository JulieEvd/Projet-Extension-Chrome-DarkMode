/*
(function () {
    //Pour remettre la teinte initial, nous modifions la valeur à 0, en cliquant sur off
    document.querySelector("html").style.filter = "invert(0) hue-rotate(180deg)";

    let media = document.querySelectorAll("img, picture, vidéo"); 

    media.forEach ((mediaItem)=>{
        mediaItem.style.filter = "invert(0) hue-rotate(180deg)";

    })
})();    
*/